generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "windows", "debian-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url = "postgresql://heather_admin:npg_YBGUs57ifpLk@ep-autumn-mud-aepfendo-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require&pgbouncer=true"
  directUrl = "postgresql://heather_admin:npg_YBGUs57ifpLk@ep-autumn-mud-aepfendo.us-east-2.aws.neon.tech/neondb?sslmode=require"
}

model User {
  id          String   @id @default(uuid())
  firstName   String
  lastName    String
  email       String   @unique
  password    String
  dateAdded   DateTime @default(now())
  dateUpdated DateTime @updatedAt @db.Date
  company     Company? @relation(fields: [companyId], references: [id])
  companyId   String?
  isAdmin     Boolean  @default(false)
}

model Company {
  id            String   @id @default(uuid())
  name          String   @unique
  phoneNumber   String   @unique
  streetAddress String
  city          String
  state         String
  zip           Int
  dateAdded     DateTime @default(now())
  dateUpdated   DateTime @updatedAt
  employees     User[]
}

model Token {
  id           Int      @id @default(autoincrement())
  accessToken  String? // access token from QuickBooks
  refreshToken String // refresh token
  realmId      String? // qb company ID
  dateAdded    DateTime @default(now())
  dateUpdated  DateTime @updatedAt
}
